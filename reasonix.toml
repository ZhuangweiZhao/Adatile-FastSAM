# Reasonix configuration.
# Resolution order: flag > ./reasonix.toml > ~/AppData/Roaming/reasonix/config.toml > built-in defaults.
# Secrets come from the environment via api_key_env; never put keys here.

config_version = 3   # schema marker for diagnostics; old versions may ignore it
default_model = "deepseek-flash"
# language      = "zh"   # ui/model language; empty = auto-detect from $LANG / $REASONIX_LANG

[agent]
# system_prompt = """..."""   # omit to use the built-in prompt for this version
# system_prompt_file = "prompts/system.md"   # overrides system_prompt when set
# max_steps         = 0   # executor tool-call rounds; 0 = no limit
# planner_max_steps = 12   # planner read-only tool-call rounds; 0 = no limit
temperature       = 0.0
auto_plan   = "off"   # off|on; off keeps plan mode manual
# reasoning_language = "zh"   # visible reasoning language: auto|zh|en
# auto_plan_classifier = "deepseek-flash"   # optional; only used for borderline tasks
soft_compact_ratio  = 0.5   # notice only; keeps cache-first prefix intact
compact_ratio       = 0.8   # try compacting when prompt reaches this fraction
compact_force_ratio = 0.9   # force compacting at this high-water mark
# keep                = ["errors"]   # compaction keep policy: errors, user_marked
# recent_keep         = 2   # minimum recent messages kept verbatim
cold_resume_prune   = true   # elide stale tool results when reopening a session past the provider cache window
# planner_model = "mimo"   # optional: enable two-model collaboration
# subagent_model = "deepseek-pro"   # optional default for runAs=subagent skills
# subagent_models = { review = "deepseek-pro", security_review = "deepseek-pro" }   # per-skill overrides
# subagent_effort = "high"   # optional default effort for subagents
# subagent_efforts = { review = "max", task = "high" }   # per-tool/skill effort overrides
# output_style = "explanatory"   # explanatory | learning | concise | custom; empty = default

[tools]
enabled = []   # empty = all built-in tools
bash_timeout_seconds = 120   # foreground safety cap; set 0 for no tool-local cap

[tools.background_jobs]
stalled_warning_seconds = 900   # warn once per background job after this many quiet seconds; 0 disables

[lsp]
enabled = true   # language server tools; servers launch lazily when used
# [lsp.servers.go]
# command = "gopls"
# args = []
# extensions = [".go"]

[skills]
# paths = ["~/my-skills", "../shared/skills"]   # extra custom skill roots
# excluded_paths = ["~/.agents/skills"]   # hide convention roots without deleting folders
# max_depth = 3   # nested scan depth; set 1 for legacy root-only discovery
# disabled_skills = ["review"]   # hide noisy or unwanted skills

[permissions]
# Per-call gating. mode = writer fallback when no rule matches: ask|allow|deny.
# Readers always default to allow. Precedence: deny > ask > allow > fallback.
# Rules are "Tool" or "Tool(specifier)"; e.g. Bash(go test:*), Edit(src/**).
mode  = "ask"
# deny = ["Bash(rm -rf*)", "Bash(git push*)"]   # hard-blocked in every mode
allow = ["Bash(git branch:*)", "Bash(git diff --stat HEAD~15..HEAD --name-only 2>$null)", "Bash(git log --oneline -1 HEAD~15..HEAD --format=\"%H %s\" 2>$null; git log --oneline -1 --format=\"%H %s\" 2>$null)", "Bash(Get-ChildItem -Name:*)", "Bash(cd E:\\A_postgraduate_stude\\AdaTile-FastSAM; python -m pytest tests/ -v --tb=short --timeout=30 2>&1 | Select-Object -First 30)", "Bash(cd E:\\A_postgraduate_stude\\AdaTile-FastSAM; python -m pytest tests/ -v --tb=short 2>&1 | Select-Object -First 50)", "Edit", "Bash(Get-Content \"E:\\A_postgraduate_stude\\AdaTile-FastSAM\\AGENTS.md\" -Encoding UTF8 | Select-Object -Skip 9 -First 2)", "Bash(Get-Content \"E:\\A_postgraduate_stude\\AdaTile-FastSAM\\AGENTS.md\" -Encoding UTF8 | Select-Object -Skip 19 -First 2 | ForEach-Object { [System.BitConverter]::ToString([System.Text.Encoding]::UTF8.GetBytes($_)) })", "Bash($content = Get-Content \"E:\\A_postgraduate_stude\\AdaTile-FastSAM\\AGENTS.md\" -Encoding UTF8 -Raw; $lines = $content -split '\\r?\\n'; $lines[21])", "Bash($bytes = [System.IO.File]::ReadAllBytes(\"E:\\A_postgraduate_stude\\AdaTile-FastSAM\\AGENTS.md\"); $text = [System.Text.Encoding]::UTF8.GetString($bytes); $lines = $text -split '\\r?\\n'; $lines[21])", "Bash($content = Get-Content \"E:\\A_postgraduate_stude\\AdaTile-FastSAM\\AGENTS.md\" -Encoding UTF8 -Raw; $content = $content -replace 'isaid_tiles\\.py.*', \"isaid_tiles.py       鉁?FastISAIDTileDataset (pre-cut 1024×1024 tiles)`r`n鈹?  鈹溾攢鈹€ loveda_tiles.py      鉁?LoveDA land-cover tiles (7-class dense, SSI<50)`r`n鈹?  鈹溾攢鈹€ nwpu.py              鉁?NWPU-VHR-10 bbox-based weak masks (10-class)`r`n鈹?  鈹斺攢鈹€ vaihingen_tiles.py   鉁?Vaihingen tiles (6-class dense semantic)\"; [System.IO.File]::WriteAllText(\"E:\\A_postgraduate_stude\\AdaTile-FastSAM\\AGENTS.md\", $content, [System.Text.UTF8Encoding]::new($false)))", "Bash(cd E:\\A_postgraduate_stude\\AdaTile-FastSAM; python -c @'\nimport re\n\nwith open(\"AGENTS.md\", \"r\", encoding=\"utf-8\") as f:\n    content = f.read()\n\n# Fix 1: Update losses from \"skeleton only\" to reflect actual state\nold = \"鈹溾攢鈹€ losses/           猬?Loss functions (skeleton only)\"\nnew = \"鈹溾攢鈹€ losses/           鉁?FocalLoss, DiceLoss, CombinedLoss\"\ncontent = content.replace(old, new)\n\n# Fix 2: Add render.py and label_mapping.py to utils\nold2 = \"    鈹斺攢鈹€ seed.py       鉁?Unified set_seed() with cuDNN deterministic\"\nnew2 = \"    鈹溾攢鈹€ seed.py          鉁?Unified set_seed() with cuDNN deterministic\\n    鈹溾攢鈹€ label_mapping.py 鉁?Per-split category ID mapping for iSAID\\n    鈹斺攢鈹€ render.py        鉁?Shared render_semantic_mask() (canonical def)\"\ncontent = content.replace(old2, new2)\n\n# Fix 3: Add fdr_predictor.py to train/\nold3 = \"鈹?  鈹斺攢鈹€ train_b04.py                 B-04 end-to-end: FDR + Decoder training\"\nnew3 = \"鈹?  鈹溾攢鈹€ train_b04.py                 B-04 end-to-end: FDR + Decoder training\\n鈹?  鈹斺攢鈹€ fdr_predictor.py             FDR wrapper (frozen MV3 + DensityHead)\"\ncontent = content.replace(old3, new3)\n\n# Fix 4: Add instance/ experiments and new paper_b experiments between paper_b and diag\nold4 = \"鈹?鈹溾攢鈹€ diag/                            # Diagnostics (paper-b branch)\"\nnew4 = \"\"\"鈹?鈹溾攢鈹€ instance/                        # C-series few-shot instance segmentation\n鈹?  鈹溾攢鈹€ eval_c02a_fastsam_fewshot.py       FastSAM + Prototype Matching (3-class iSAID)\n鈹?  鈹溾攢鈹€ eval_c02b_decoder_fewshot.py       FastSAM + Decoder few-shot\n鈹?  鈹溾攢鈹€ eval_c03_catsam_fewshot.py         CAT-SAM baseline comparison\n鈹?  鈹溾攢鈹€ eval_c04_full_fewshot.py           Full 15-class few-shot instance seg (Phase D)\n鈹?  鈹溾攢鈹€ eval_fastsam_zero_shot.py          FastSAM zero-shot baseline\n鈹?  鈹斺攢鈹€ run_c01_sweep.py                   Hyperparameter sweep runner\n鈹?\n鈹溾攢鈹€ diag/                            # Diagnostics (paper-b branch)\"\"\"\ncontent = content.replace(old4, new4)\n\n# Fix 5: Add new paper_b experiments  \nold5 = \"鈹?  鈹斺攢鈹€ eval_b03_router_architecture.py     FDR vs Edge ablation: R0/R1/R2/R3, Density鈮燛dge proof\"\nnew5 = \"\"\"鈹?  鈹溾攢鈹€ eval_b03_router_architecture.py     FDR vs Edge ablation: R0/R1/R2/R3, Density鈮燛dge proof\n鈹?  鈹溾攢鈹€ eval_b05_oracle_importance.py       Oracle tile importance: multi-metric analysis\n鈹?  鈹溾攢鈹€ eval_b05_5_tile_size.py             Tile size × importance interaction\n鈹?  鈹溾攢鈹€ eval_b06_contribution_granularity.py  Contribution imbalance vs tile granularity\n鈹?  鈹溾攢鈹€ eval_b06_decoder_diag.py            Decoder diagnostics: per-class IoU, failure modes\n鈹?  鈹溾攢鈹€ eval_b07_contribution_gt.py          GT contribution analysis\n鈹?  鈹溾攢鈹€ eval_b07_fdr_data_efficiency.py      FDR sample efficiency (1%-100% data)\n鈹?  鈹溾攢鈹€ eval_b08_fastsam_fewshot.py          FastSAM-FSS: few-shot semantic seg (LoveDA/Vaihingen)\n鈹?  鈹溾攢鈹€ eval_b09_nwpu_fewshot.py             FastSAM-FSS on NWPU-VHR-10 (bbox weak masks)\n鈹?  鈹斺攢鈹€ eval_paper_b_pipeline.py             Unified Paper B pipeline (multi-dataset)\"\"\"\ncontent = content.replace(old5, new5)\n\n# Fix 6: Update Decoder line\nold6 = \"B-04 LightDecoder (for binary segmentation):\"\nnew6 = \"B-04 Decoders:\"\ncontent = content.replace(old6, new6)\n\nwith open(\"AGENTS.md\", \"w\", encoding=\"utf-8\", newline=\"\") as f:\n    f.write(content)\n\nprint(\"Done. Changes applied.\")\n'@)", "Bash(cd E:\\A_postgraduate_stude\\AdaTile-FastSAM; python _update_agents.py AGENTS.md)", "Bash(cd E:\\A_postgraduate_stude\\AdaTile-FastSAM; python -c \"\nimport sys, io\nsys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')\nwith open('AGENTS.md', 'rb') as f:\n    data = f.read()\n# Find losses line\nidx = data.find(b'losses')\nif idx >= 0:\n    chunk = data[idx-20:idx+80]\n    print('Raw bytes around losses:', chunk.hex(' '))\n    print('Decoded:', chunk.decode('utf-8', errors='replace'))\n\")", "Bash(cd E:\\A_postgraduate_stude\\AdaTile-FastSAM; python -c \"\nimport sys, io\nsys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')\nwith open('AGENTS.md', 'rb') as f:\n    data = f.read()\n# Find 'losses/' pattern  \nidx = data.find(b'losses/')\nif idx >= 0:\n    # Get surrounding context\n    start = max(0, idx - 30)\n    end = min(len(data), idx + 60)\n    chunk = data[start:end]\n    print(f'Found at byte {idx}')\n    print('Hex:', chunk.hex(' '))\n    print('Decoded:', chunk.decode('utf-8', errors='replace'))\nelse:\n    print('losses/ not found')\n    \n# Also find seed.py\nidx2 = data.find(b'seed.py')\nif idx2 >= 0:\n    start = max(0, idx2 - 30)\n    end = min(len(data), idx2 + 50)\n    chunk = data[start:end]\n    print(f'\\nseed.py at byte {idx2}')\n    print('Hex:', chunk.hex(' '))\n    print('Decoded:', chunk.decode('utf-8', errors='replace'))\n\")", "Bash(cd E:\\A_postgraduate_stude\\AdaTile-FastSAM; python -c \"\nimport sys, io\nsys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')\nwith open('AGENTS.md', 'rb') as f:\n    data = f.read()\n\n# Find key markers\nfor marker in [b'train_b04.py', b'diag/', b'eval_b03_router']:\n    idx = data.find(marker)\n    if idx >= 0:\n        start = max(0, idx - 40)\n        end = min(len(data), idx + 80)\n        chunk = data[start:end]\n        print(f'{marker.decode()} at {idx}:')\n        print('  Decoded:', chunk.decode('utf-8', errors='replace').replace(chr(10), '|'))\n        print()\n\")", "Bash(cd E:\\A_postgraduate_stude\\AdaTile-FastSAM; python -c \"\nimport sys, io\nsys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')\n\nwith open('AGENTS.md', 'r', encoding='utf-8', errors='replace') as f:\n    lines = f.readlines()\n\n# Show lines around deforestation to find tree structure\nfor i, line in enumerate(lines):\n    if 'losses' in line and 'skeleton' in line:\n        print(f'Line {i}: {repr(line)}')\n    if 'seed.py' in line:\n        print(f'Line {i}: {repr(line)}')\n    if 'train_b04' in line:\n        print(f'Line {i}: {repr(line)}')\n    if 'diag/' in line and 'Diagnostics' in line:\n        print(f'Line {i}: {repr(line)}')\n    if 'eval_b03_router' in line:\n        print(f'Line {i}: {repr(line)}')\n\")", "Bash(cd E:\\A_postgraduate_stude\\AdaTile-FastSAM; python -c \"\nimport sys, io\nsys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')\nwith open('AGENTS.md', 'r', encoding='utf-8', errors='replace') as f:\n    content = f.read()\n# Check if the strings exist\ntests = [\n    'losses/',\n    'skeleton only',\n    'seed.py',\n    'train_b04.py',\n    'diag/',\n    'eval_b03_router',\n]\nfor t in tests:\n    print(f'{t}: {\\\"FOUND\\\" if t in content else \\\"NOT FOUND\\\"}')\")", "Bash(cd E:\\A_postgraduate_stude\\AdaTile-FastSAM; python -c \"\nimport sys, io\nsys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')\nwith open('AGENTS.md', 'r', encoding='utf-8', errors='surrogateescape') as f:\n    content = f.read()\n# Check if key markers exist\nfor t in ['losses/', 'skeleton only', 'seed.py', 'train_b04.py', 'diag/', 'eval_b03_router']:\n    print(t + ': ' + ('FOUND' if t in content else 'NOT FOUND'))\n\")", "Bash(Remove-Item \"E:\\A_postgraduate_stude\\AdaTile-FastSAM\\_update_agents.py\":*)", "Bash(cd E:\\A_postgraduate_stude\\AdaTile-FastSAM; (Get-Content AGENTS.md -Encoding UTF8 | Select-Object -First 1), (Get-Content AGENTS.md -Encoding UTF8).Count)", "Bash(cd E:\\A_postgraduate_stude\\AdaTile-FastSAM; python -c \"\nimport sys, io\nsys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')\n\n# Check if CLAUDE.md needs the same updates\nwith open('CLAUDE.md', 'r', encoding='utf-8', errors='surrogateescape') as f:\n    content = f.read()\n\n# Quick check\nprint('Has instance/:', 'instance/' in content)\nprint('Has loveda_tiles:', 'loveda_tiles' in content)\nprint('Has B-05:', 'B-05' in content)\nprint('Has fdr_predictor:', 'fdr_predictor' in content)\n\")", "Bash(cd E:\\A_postgraduate_stude\\AdaTile-FastSAM; python _update_claude.py)", "Bash(Remove-Item \"E:\\A_postgraduate_stude\\AdaTile-FastSAM\\_update_claude.py\":*)", "Bash(cd E:\\A_postgraduate_stude\\AdaTile-FastSAM; python -c \"import sys; sys.path.insert(0, '.'); import py_compile; py_compile.compile('adatile/datasets/p4_cache.py', doraise=True); print('OK')\")"]
# ask = ["Edit(src/**)"]   # force a prompt even if otherwise allowed

[sandbox]
# Confine tool blast radius. File-writers (write_file/edit_file/multi_edit/move_file)
# may only write under workspace_root (empty = current dir) and allow_write extras.
# bash = "enforce" (default) jails each command in an OS sandbox (macOS now;
# graceful fallback elsewhere); "off" disables it. network allows egress.
# workspace_root = ""            # default: current working directory
# allow_write = ["/tmp"]          # extra dirs writers may also modify
bash    = "enforce"
network = true

[statusline]
# A custom status line: a command whose first stdout line replaces the built-in
# data row. It receives {"model","contextUsed","contextWindow","cwd"} as JSON on stdin.
# command = "my-statusline.sh"

# External MCP servers. type: "stdio" (default, a subprocess) | "http" | "sse".
# ${VAR} / ${VAR:-default} are expanded from the environment in command/args/env/url/headers.
# [[plugins]]
# name    = "example"
# command = "reasonix-plugin-example"
# [[plugins]]                                  # a remote server over Streamable HTTP
# name    = "stripe"
# type    = "http"
# url     = "https://mcp.stripe.com"
# headers = { Authorization = "Bearer ${STRIPE_KEY}" }
